input {
  file {
    type => "webapp",
    path => "/www/APP/shared/log/*"
  }
}

output {
  stdout { debug => true debug_format => "json"}

  redis {
    host => "10.188.5.120"
    data_type => "list"
    key => "logstash"
    timeout => 120
  }
}