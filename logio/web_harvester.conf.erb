exports.config = {

    // Log server host & port
    server: {
        host: '<%= logio_server %>',
        port: 8998
    },

    // Watch the following log files, defined by label:path mappings
    log_file_paths: {
        //production: '/www/APP/shared/log/production.log',
    }
}